python-binance==1.0.27
sqlalchemy==1.4.15
schedule==1.1.0
apprise==0.9.5.1
Flask==3.1.3
gunicorn==20.1.0
flask-cors==3.0.10
flask-socketio==5.6.1
python-socketio[client]==5.16.4
cachetools==4.2.2
sqlitedict==1.7.0
unicorn-binance-websocket-api==1.34.2
unicorn-fy==0.11.0
itsdangerous==2.2.0
Werkzeug==3.1.8

# eventlet 0.30.2 did `import imp`, which Python 3.12 removed; 0.41.x supports
# 3.13. Keeping it means docker-compose's `gunicorn -k eventlet` worker still
# resolves and socket.io keeps a real websocket transport rather than falling
# back to long-polling. Note that eventlet itself is now in bugfix-only
# maintenance and warns against new use -- worth revisiting.
eventlet==0.41.2
# Pinned explicitly: eventlet imports dnspython for greendns, and the 1.16.0
# that old eventlet pinned uses collections.MutableMapping, removed in 3.10.
# Without this pin an environment upgraded in place keeps the broken 1.16.0.
dnspython==2.8.0
